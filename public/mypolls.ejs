<!DOCTYPE html>
<html>
   <head>
      <title>Profile</title>
      <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
      <link href='http://fonts.googleapis.com/css?family=Pacifico' rel='stylesheet' type='text/css'>

      <link href="/css/nav.css" rel="stylesheet" type="text/css">
      <link href="/css/profile.css" rel="stylesheet" type="text/css">
      <link href="/css/list.css" rel="stylesheet" type="text/css">
      <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
   </head>
   <body>
     <nav class="navbar navbar-expand-sm">
        <ul class="navbar-nav">
          <li>
            <img src="/img/vote.png" class="nav-icon"></img>
          </li>
          <li class="nav-brand">
            <h1 id="nav-title">Voting App</h1>
          </li>
        </ul>
        <ul class="navbar-nav ml-auto">
          <a href="/">
            <li class="nav-button">Home</li>
          </a>
          <a href="/logout">
            <li class="nav-button">Logout</li>
          </a>
        </ul>
      </nav>
      <div class="container-fluid">
        <div id="profile-top-container">
          <h1 id="profile-title">Hello, <%= user.name %></h1>
          <h4 id="profile-subtitle">What would you like to do?</h4>
          <div id="profile-button-container">
            <button id="new-poll-button" class="profile-button">
              New Poll
            </button>
            <button id="my-polls-button" class="profile-button profile-selected">
              My Polls
            </button>
          </div>
        </div>
        <div id="profile-action-container">
          <div class="list-box">
            <h3 class="list-title">My Polls</h3>
            <% if (polls.length === 0) { %>
              <h6 class="list-subtitle">No polls to show</h6>
            <% } else { %>
              <div class="row">
              <% for (var i = 0; i < polls.length; i++) { %>
                <div class="col-md-3 poll-outline">
                  <% var link = "/poll/" + polls[i]._id %>
                  <a href=<%= link %> class="poll-link">
                    <div class="poll-item">
                      <h4 class="poll-title"><%= polls[i].title %></h6>
                      <h6 class="poll-subtitle">Votes: <%= polls[i].total_votes %></h6>
                    </div>
                  </a>
                </div>
              <% } %>
              </div>
            <% } %>
          </div>
        </div>
        <%- include bottombar.ejs %>
      </div>
   </body>
   <script>
    $(document).ready(function() {
      $("#new-poll-button").on("click", function() {
        window.location.href = '/createpoll';
      });
    });
   </script>
</html>
